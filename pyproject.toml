[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ecc"
version = "0.1.0"
description = "Toy elliptic curve playground over interchangeable fields"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
